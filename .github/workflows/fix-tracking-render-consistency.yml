name: Fix tracking render consistency

on:
  push:
    branches:
      - agent/motion-tracking-auto-reframe
    paths:
      - '.github/workflows/fix-tracking-render-consistency.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/motion-tracking-auto-reframe
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Patch renderer consistency
        shell: pwsh
        run: python scripts/fix-tracking-render-consistency.py
      - name: Commit production patch and remove helpers
        shell: pwsh
        run: |
          Remove-Item scripts/fix-tracking-render-consistency.py
          Remove-Item .github/workflows/fix-tracking-render-consistency.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'Keep auto reframe consistent in preview and overlay render'
          git push origin HEAD:agent/motion-tracking-auto-reframe
