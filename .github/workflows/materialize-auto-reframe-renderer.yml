name: Materialize Auto Reframe renderer

on:
  push:
    branches:
      - agent/motion-tracking-auto-reframe
    paths:
      - '.github/workflows/materialize-auto-reframe-renderer.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/motion-tracking-auto-reframe
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply Auto Reframe renderer patch
        shell: pwsh
        run: python scripts/materialize-auto-reframe-renderer.py
      - name: Commit production files and remove materializer
        shell: pwsh
        run: |
          Remove-Item scripts/materialize-auto-reframe-renderer.py
          Remove-Item .github/workflows/materialize-auto-reframe-renderer.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'Render tracked Auto Reframe crop'
          git push origin HEAD:agent/motion-tracking-auto-reframe
