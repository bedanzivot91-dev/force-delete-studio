name: Materialize final tracking gaps

on:
  push:
    branches:
      - agent/motion-tracking-auto-reframe
    paths:
      - '.github/workflows/materialize-tracking-final-gaps.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/motion-tracking-auto-reframe
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Materialize final correctness and payload fixes
        shell: pwsh
        run: python scripts/materialize-tracking-final-gaps.py
      - name: Commit production patch and remove helpers
        shell: pwsh
        run: |
          Remove-Item scripts/materialize-tracking-final-gaps.py
          Remove-Item .github/workflows/materialize-tracking-final-gaps.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'Finish tracking stabilization consistency and release payload'
          git push origin HEAD:agent/motion-tracking-auto-reframe
