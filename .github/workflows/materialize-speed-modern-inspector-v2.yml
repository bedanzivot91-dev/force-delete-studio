name: Materialize modern inspector velocity v2

on:
  push:
    branches:
      - agent/velocity-speed-curves-v2
    paths:
      - '.github/workflows/materialize-speed-modern-inspector-v2.yml'

permissions:
  contents: write

jobs:
  materialize:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/velocity-speed-curves-v2
          fetch-depth: 0
      - name: Add velocity to active modern inspector
        shell: pwsh
        run: .github/scripts/materialize-speed-modern-inspector-v2.ps1
