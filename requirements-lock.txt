# Resolved lock for requirements-core.txt, generated by actually running
# `pip install` against PyPI in this session on 2026-07-31 (python3.13 venv)
# and freezing the result -- these are real resolved versions, not guesses.
greenlet==3.5.4
playwright==1.61.0
pyee==13.0.1
typing_extensions==4.16.0

# requirements-ai.txt (faster-whisper, ctranslate2, audio-separator) was NOT
# resolved here: those packages pull GPU/CPU tensor wheels in the hundreds
# of MB to multiple GB range (torch, onnxruntime, ctranslate2 native libs),
# which this sandboxed session's outbound network policy does not reliably
# support downloading in one pass. The GitHub Actions Windows CI job
# (.github/workflows/windows-build.yml) resolves and locks requirements-ai.txt
# for real on a runner with full network access; see that workflow's
# artifact for the actual resolved AI lock file once it has run.
